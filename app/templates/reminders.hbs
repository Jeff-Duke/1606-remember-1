<div>
  <ul>
    {{#each model as |reminder|}}
      <li>
        {{#link-to 'reminders.reminder' reminder}}
          <p class="spec-reminder-item">
            {{reminder.title}}
          </p>
        {{/link-to}}
        <p class='list-date'>{{reminder.date}}</p>
      </li>
    {{/each}}
  </ul>
  {{#link-to 'reminders.new-reminder' new-reminder}}<button>New</button>{{/link-to}}
</div>

{{outlet}}
